import { TelemetryClick } from './telemetry-click';

describe('TelemetryClick', () => {
  it('should create an instance', () => {
    const directive = new TelemetryClick();
    expect(directive).toBeTruthy();
  });
});
