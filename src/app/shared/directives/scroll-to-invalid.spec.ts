import { ScrollToInvalid } from './scroll-to-invalid';

describe('ScrollToInvalid', () => {
  it('should create an instance', () => {
    const directive = new ScrollToInvalid();
    expect(directive).toBeTruthy();
  });
});
