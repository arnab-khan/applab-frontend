import { SanitizeInput } from './sanitize-input';

describe('SanitizeInput', () => {
  it('should create an instance', () => {
    const directive = new SanitizeInput();
    expect(directive).toBeTruthy();
  });
});
